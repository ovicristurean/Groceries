CREATE TABLE Recipe (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    ingredients TEXT NOT NULL,
    recipeName TEXT NOT NULL
);

getRecipes:
SELECT * FROM Recipe;

insertRecipe:
INSERT INTO Recipe(id, recipeName, ingredients)
VALUES (?,?, ?);

getRecipeById:
SELECT *
FROM Recipe
WHERE id = :id;
